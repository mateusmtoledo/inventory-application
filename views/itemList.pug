extends layout

block content
  h2= title
  div.item-list
    for item in items
      div.item
        a(href=item.url)
          img(src=item.image, alt=item.name)
          p= item.name
          p= item.price
          p= item.numberInStock
